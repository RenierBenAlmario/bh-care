/**
 * Enhanced Theme for BHCARE System
 * This script is now simplified to just apply the enhanced theme directly
 */
document.addEventListener('DOMContentLoaded', function() {
    // The enhanced theme is now applied directly via CSS
    // This script is kept for potential future theme management
    console.log('Enhanced theme applied');
});
