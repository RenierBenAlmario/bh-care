@page
@model Barangay.Pages.User.ExecuteSqlModel
@{
    ViewData["Title"] = "Execute SQL";
}

<div class="container">
    <h1>Database Update Results</h1>
    
    @if (Model.Success)
    {
        <div class="alert alert-success">
            <h4>SQL executed successfully!</h4>
            <p>The database schema has been updated.</p>
        </div>
    }
    else
    {
        <div class="alert alert-danger">
            <h4>Error executing SQL</h4>
            <p>@Model.ErrorMessage</p>
        </div>
    }
    
    <div class="mt-4">
        <a href="/User/UserDashboard" class="btn btn-primary">Return to Dashboard</a>
    </div>
</div> 