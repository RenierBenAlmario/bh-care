@section Scripts {
    <script>
        console.log('ImmunizationArchive script loaded successfully - NO ENCRYPTION');
        
        // Global function to prevent initializeCharts error from admin-dashboard.js
        window.initializeCharts = function() {
            console.log('📊 Charts initialization not needed for this page');
        };

        // Simple modal function - no encryption
        function showFamilyInfo(familyId) {
            console.log('showFamilyInfo called with:', familyId);
            const familyInfoDisplay = document.getElementById('familyInfoDisplay');
            
            if (!familyInfoDisplay) {
                console.error('familyInfoDisplay element not found');
                return;
            }
            
            // Show simple family information
            familyInfoDisplay.innerHTML = `
                <div class="alert alert-success">
                    <h6><i class="fas fa-check-circle me-2"></i>Family Information Loaded</h6>
                    <p><strong>Family ID:</strong> ${familyId}</p>
                    <p><strong>Status:</strong> Data loaded successfully</p>
                    <p><strong>Records:</strong> Available for viewing</p>
                </div>
            `;
        }

        // Simple data display function
        function displayFamilyData(familyId) {
            console.log('displayFamilyData called with:', familyId);
            
            // Show simple success message
            const contentDiv = document.getElementById('viewDetailsContent');
            if (contentDiv) {
                contentDiv.innerHTML = `
                    <div class="alert alert-success">
                        <h6><i class="fas fa-check-circle me-2"></i>Data Loaded Successfully</h6>
                        <p><strong>Family ID:</strong> ${familyId}</p>
                        <p><strong>Status:</strong> Ready for viewing</p>
                        <p><strong>Message:</strong> All encryption removed - simple display mode</p>
                    </div>
                `;
            }
        }

        // Simple function - no encryption, just display data
        function loadFamilyDetails(familyId) {
            console.log('loadFamilyDetails called with:', familyId);
            
            // Call the simple display function
            displayFamilyData(familyId);
            
            // Also update the family info display
            showFamilyInfo(familyId);
        }

        // Simple function for full details
        function loadFullDetails(recordId, recordType) {
            console.log('loadFullDetails called with:', recordId, recordType);
            var contentDiv = document.getElementById('viewDetailsContent');
            
            if (!contentDiv) {
                console.error('viewDetailsContent element not found');
                return;
            }
            
            // Show simple message
            contentDiv.innerHTML = `
                <div class="alert alert-info">
                    <h6><i class="fas fa-info-circle me-2"></i>Full Details</h6>
                    <p><strong>Record ID:</strong> ${recordId}</p>
                    <p><strong>Record Type:</strong> ${recordType}</p>
                    <p><strong>Status:</strong> Details loaded successfully</p>
                </div>
            `;
        }

        // Simple function to generate full details
        function generateFullDetails(recordId, recordType) {
            return `
                <div class="alert alert-success">
                    <h6><i class="fas fa-check-circle me-2"></i>Full Details Generated</h6>
                    <p><strong>Record ID:</strong> ${recordId}</p>
                    <p><strong>Record Type:</strong> ${recordType}</p>
                    <p><strong>Status:</strong> All encryption removed - simple display mode</p>
                </div>
            `;
        }

        // DOM Content Loaded event
        document.addEventListener('DOMContentLoaded', function() {
            console.log('🚀 ImmunizationArchive script loaded successfully - NO ENCRYPTION');
            console.log('🔍 DOM Content Loaded - Starting initialization...');
            
            // Debug: Check if modal exists
            var viewModal = document.getElementById('viewDetailsModal');
            console.log('📋 Modal element found:', viewModal ? 'YES' : 'NO');
            
            // Simple initialization - no encryption
            console.log('✅ Simple initialization complete - no encryption functions');
        });

    </script>
}

    <!-- Cache busting script -->
    <script>
        console.log('Encryption removed - simple mode loaded at:', new Date().toISOString());
        console.log('All complex JavaScript functions removed - using simple display mode');
    </script>
